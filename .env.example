# Environment variables for url-shortener

# Database configuration (for local development with Docker)
DATABASE_URL=postgres://urluser:urlpass@localhost:5432/url_shortener?sslmode=disable

# Server configuration
PORT=8080

# For production, use a secure DATABASE_URL like:
# DATABASE_URL=postgres://username:password@host:port/database?sslmode=require